# UD 03.03 -Caso practico 01 - Practica de

# comandos en contendor Docker.

## Crear el contenedor: Ejecuta el siguiente comando en tu terminal para crear un contenedor

## llamado "ejercicio" e iniciar una sesión interactiva:

- docker run -it --name ejercicio ubuntu /bin/bash
- creacion del contenedor
Captura.PNG

## Mediante ShellScript (Recomendado)

## Puedes automatizarlo con este bucle directamente en la terminal del contenedor:

## Bash

for i in {1..10}
do
if [ $i -lt 10 ]; then
mkdir /root/0$i
else
mkdir /root/$i
fi
done
Captura_1.PNG

